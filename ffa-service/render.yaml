services:
  - type: web
    name: wscprep
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://ws-cprep.vercel.app
